{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "types": []
  },
  "include": ["tmp", "../src/components"],
  "exclude": [] // override parent exclude — the parent excludes evals/tmp; the judge needs to type-check it
}
